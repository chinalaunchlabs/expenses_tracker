kind: pipeline
type: docker
name: default

steps:
- name: build and publish
  image: plugins/docker
  settings:
    repo: registry.koda.ws/expenses/api
    registry: registry.koda.ws
    username: testuser
    password: testpassword
    tags:
      - latest
  commands:
  - docker build -t expenses/api .
  - docker push registry.koda.ws/expenses/api